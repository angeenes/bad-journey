---
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    You are now logged in with your DISCORD account
</body>
</html>

<style>
    body {
        background-color: var(--color-primary);
        color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100%;
    }
</style>

<script>

    import { GoogleOauth } from "../../../assets/types/googleOauth.js";
    import { API_URL } from "../../../consts.js";

    function getUrlParameters(): GoogleOauth {
        var params = {};
        window.location.search.replace(/[?&]+([^=&]+)=([^&]*)/gi, (str?: string, key: string, value: string) => {
            params[key] = value;
        });
        console.log('params', params);
        return params as GoogleOauth;
    }

    function retrieveUserInfos() {
        const params: GoogleOauth = getUrlParameters();
        fetch(`${API_URL}auth/google/callback?access_token=${params.access_token}`)
            .then(res => res.json())
            .then(res => {
                console.log(res);
                localStorage.setItem("jwt", res.jwt);
                localStorage.setItem("user", JSON.stringify(res.user));
                setTimeout(() => {
                    window.location.href = "/account";
                }, 2500);
            })
            .catch(err => console.log(err));
    }

    retrieveUserInfos();
</script>