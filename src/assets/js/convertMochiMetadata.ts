import { ImageObject } from "../types/ImageObject";

export default function convertMochiMetadata(metadata) {

    let object: ImageObject = {} as ImageObject;

    console.log('convertMochiMetadata', metadata);

    return object;
}

